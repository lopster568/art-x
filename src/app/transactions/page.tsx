const Page = () => {
    return (
        <div>
            My Transaction Copy the transaction detail component and map all the user transactions on the page.
        </div>
    );
}

export default Page;